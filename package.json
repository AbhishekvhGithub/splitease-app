{
  "name": "splitease",
  "version": "1.0.0",
  "description": "A Splitwise-like expense splitting app",
  "main": "backend/server.js",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "install": "npm install --prefix backend",
    "start": "node backend/server.js",
    "dev": "cd backend && npm run dev",
    "build": "echo 'Build complete'"
  }
}
